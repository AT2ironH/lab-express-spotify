<h1>Albums</h1>
{{#each albums}}
<div>{{this.name}}</div>
<img src="{{this.images.1.url}}">
<a href="/tracks/{{this.id}}"><button type="submit" class="btn btn-danger">View Tracks</button></a>
{{/each}}

